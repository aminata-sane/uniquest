using UnityEngine;

// Script minimal pour identifier les erreurs de compilation Unity
public class ErrorDetector : MonoBehaviour
{
    void Start()
    {
        Debug.Log("Si ce message apparaît, Unity compile sans erreur !");
    }
}
